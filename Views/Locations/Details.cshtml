@model ds3Wiki.Models.Location

@{
    ViewData["Title"] = "Details";
}

<h1>Детали</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Bonfire)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Bonfire)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Game_Info)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Game_Info.Name_of_game)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-dark" asp-action="Index">Вернуться</a>
    @if (User.IsInRole("admin") || User.IsInRole("moderator"))
    {
        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Изменить</a>
    }
</div>