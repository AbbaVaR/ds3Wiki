@model ds3Wiki.Models.NPC_location

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>NPC_location</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LocationId" class="control-label"></label>
                <select asp-for="LocationId" class ="form-control" asp-items="ViewBag.LocationId"></select>
            </div>
            <div class="form-group">
                <label asp-for="EnemyId" class="control-label"></label>
                <select asp-for="EnemyId" class ="form-control" asp-items="ViewBag.EnemyId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CharacterId" class="control-label"></label>
                <select asp-for="CharacterId" class ="form-control" asp-items="ViewBag.CharacterId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
