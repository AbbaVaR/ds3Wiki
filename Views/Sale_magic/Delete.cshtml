@model ds3Wiki.Models.Sale_magic

@{
    ViewData["Title"] = "Delete";
}

<h1>Удаление</h1>

<h3>вы уверены?</h3>
<div>
    <h4>Sale_magic</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Character)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Character.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Magic)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Magic.Id)
        </dd>
    </dl>

    <form asp-action="Delete">
        <a class="btn btn-dark" asp-action="Index">Вернуться</a>
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
    </form>
</div>
